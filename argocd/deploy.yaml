#@ load("@ytt:data", "data")
apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: fetimetracking
spec:
  template:
    spec:
      containers:
      - image: #@ data.values.APP_IMAGE_DIGEST

---
timeout: 180
memory: 768M
instances: 1
env:
    spring.datasource.platform: h2
    #JBP_CONFIG_INTROSCOPE_AGENT: '{repository_root: "https://s3.amazonaws.com/pcf-softwares-57/ca", version: 10.0.+}'
    JBP_CONFIG_INTROSCOPE_AGENT: '{version: 10.0.+}'
applications:
 - name: timetracking
   host: timetrackng
#   random-route: true
   #buildpack: https://github.com/cloudfoundry/java-buildpack.git
   health-check-type: http
   health-check-http-endpoint: /health
   path: target/fe-time-tracking-1.0.0-SNAPSHOT.jar
   services: [ introscope ]
